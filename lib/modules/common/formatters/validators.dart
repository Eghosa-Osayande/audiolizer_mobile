import 'package:flutter_multi_formatter/flutter_multi_formatter.dart';

// String? numericValidator(String? v) {
//   if (toNumber(v) == null) return "Value must be numeric";
// }
// String? numericValidatorNonZero(String? v) {
//   if (toNumber(v) == null) return "Value must be numeric";
//   if ((toNumber(v)??0) < 1) return "Value can't be 0";
// }

// num? toNumber(String? v) {

//   return num.tryParse(toNumericString(v,allowPeriod: true));
// }
